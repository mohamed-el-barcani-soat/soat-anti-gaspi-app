import { useParams } from "react-router-dom";

const Offer = () => {
    const {id} = useParams();

    return <div>
        offer page not implement yet, id is '{id}'
    </div>
}

export default Offer;